{
  "model_name": "BAAI/bge-m3",
  "learning_rate": 1e-05,
  "epochs": 2,
  "batch_size": 4,
  "gradient_accumulation_steps": 8,
  "warmup_ratio": 0.1,
  "fp16": true,
  "gradient_checkpointing": true,
  "eval_split": 0.05,
  "loss": "MultipleNegativesRankingLoss",
  "notes": "anchor+positive+negative triplets; first hard negative per triplet (rank 5 from FAISS) is passed as the `negative` column. MNRL also uses remaining in-batch samples as negatives."
}